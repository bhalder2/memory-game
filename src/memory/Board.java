package memory;

/**
 *  Maintains the current state of the board using a hash table of
 *  Card objects.
 *  @author Brice Halder
 */
class Board {

    Board(int numCards) {
    }

    boolean hasUnmatched() {
        return true;
    }

    void flip(String coord1, String coord2) {
    }

    boolean isMatch(String coord1, String coord2) {
        return true;
    }
}
